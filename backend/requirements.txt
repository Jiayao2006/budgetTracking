fastapi==0.104.1
uvicorn[standard]==0.24.0
# Upgrade SQLAlchemy to support Python 3.13
sqlalchemy==2.0.35
# Upgrade pydantic to a version whose bundled pydantic-core wheels cover newer Python versions (3.11/3.12) to avoid source builds
pydantic==2.8.2
python-multipart==0.0.6
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-dotenv==1.0.0
# Include both PostgreSQL drivers for compatibility
# Main driver - modern psycopg v3
psycopg[binary]==3.2.9
# Fallback for SQLAlchemy if needed
psycopg2-binary==2.9.9
alembic==1.13.0
